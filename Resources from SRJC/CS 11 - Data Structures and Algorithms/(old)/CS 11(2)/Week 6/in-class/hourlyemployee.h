


using namespace std;

int main)_
{
    hourlyEmp arnold("Arnold Jones")
    arnold.setHours(10);
    cout <<
}










employee::employee()





































hourlyEmp::hourlyEmp(const string& newName,
  const string& newSsn,
  double newPayRate,
  double newHours)

 : employee(newName, newSsn),
    payRate(newPayRate),
    hours(newHours),
    netPay(hours * payRate)


/*
{
  payRate = newPayrate;
  hours = newHours;
  netPay = hours * payRate;
}
